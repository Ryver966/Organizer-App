var chai = require('chai');
var assert = chai.assert;
var mainFile = require('../organizer.js');

describe('Test of tests', function(){
    it('should works', function(){
        const arr = [];

        assert.equal(arr.length, 0);
    })
})

